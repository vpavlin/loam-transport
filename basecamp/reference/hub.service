[Unit]
Description=scala headless hub (logoscore daemon + scala_core, pinned to the logos.dev fleet)
After=network-online.target
Wants=network-online.target
[Service]
Type=simple
ExecStart=%h/scala/hub/hub-run.sh
Restart=always
RestartSec=5
TimeoutStopSec=30
[Install]
WantedBy=default.target
